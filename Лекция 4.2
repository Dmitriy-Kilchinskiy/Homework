import random


def sort(el):
    n = len(el)
    for i in range(n - 1):
        for j in range(0, n - i - 1):
            if el[j] > el[j + 1]:
                el[j], el[j + 1] = el[j + 1], el[j]
    return el

while True:
    try:
        N = int(input("Введите нужное количество элементов массива: "))
        break
    except (TypeError, ValueError) as e:
        print("Ошибка", e)

el = []
for x in range(0, N):
    el.append(random.randint(0, 1000))


print(*sort(res))
